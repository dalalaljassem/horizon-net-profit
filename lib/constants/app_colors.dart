import 'package:flutter/material.dart';

class AppColors {
  // Background colors
  static const Color background = Colors.white;
  static const Color cardBackground = Colors.white;
  static const Color sheetBackground = Colors.white;

  // Text colors
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Color(0xFF666666);
  static const Color textLight = Color(0xFF999999);

  // Button colors
  static const Color buttonPrimary = Colors.black;
  static const Color buttonSecondary = Color(0xFFE0E0E0);

  // Accent colors
  static const Color percentageBackground = Color(0xFFE3F2FD);
  static const Color percentageText = Color(0xFF1976D2);

  // Border colors
  static const Color border = Color(0xFFE8E8E8);
  static const Color chipBorder = Color(0xFFE8E8E8);

  // Icon colors
  static const Color iconGrey = Color(0xFF757575);

  // Handle color
  static const Color sheetHandle = Color(0xFFBDBDBD);
}
